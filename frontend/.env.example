# true  -> render frontend/src/mockData.js without calling the backend
# false -> POST /api/analyse
VITE_USE_MOCK=true

# Where the dev-server proxy sends /api requests.
VITE_API_TARGET=http://localhost:5000
